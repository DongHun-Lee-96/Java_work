package stream;

public class stream {
	/*
	 자료의 대상과 관계없이 동일한 연산을 수행: 배열, 컬렉션을 대상으로 동일한 연상을 수항하고 일관성 있는 연산으로 자료의 처리를 쉽고 간단하게한다
	 한번생성하고 사용한 스트림은 재사용 불가: 다른 연산을 위해서는 새로운 스트림을 생상해야함
	 스트림 연산은 기존 자료를 변경하지 않음: 별도의 메모리 공간 사용으로 기존 자료 변경하지 않음
	 중간 연산자, 최종 연산자: 중간 연산은 여러개 적용가능하나 최종연산은 마지막에 하번만 적용됨,
	 최종 연산이 호출되어야 중간 연산의 결과가 모두 적용됨 이를 지연연산이라고 한다
	 */
	
	/*
	 
	 */
}
